(function () {

    /*
	var windowWidth = window.innerWidth;
    var windowHeight = window.innerHeight;
    var page_container = document.querySelector('.page-container');
    var page_back = document.querySelector('.page-back');

    page_container.addEventListener("mousemove", function(e) {

        var moveX = ((windowWidth / 2) - e.pageX) * 0.1;
        var moveY = ((windowHeight / 2) - e.pageY) * 0.1;

        page_back.style.marginLeft = moveX + "px";
        page_back.style.marginTop = moveY + "px";

    });
    */


})();